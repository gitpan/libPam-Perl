Beta quality code.

Installer does not fully work.

ln -s ....auto/Authen/PAM/Module.so /lib/security/pam_perl.so

Assumes that there is only one pam module written in perl, that it is not called from perl or an application linked against perl, or from a multi-threaded application. resurch in progress to corect thes assumptions.

now in final name space.

your module must be under namespace.
